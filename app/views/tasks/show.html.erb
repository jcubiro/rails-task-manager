<div class="container">
  <h1><%= @task.title %></h1>
  <p><%= @task.details %></p>
  <%= button_to "Delete", task_path(@task), method: :delete, data: { confirm: "Are you sure you want to delete this?" }, class: "btn btn-danger" %>
</div>
